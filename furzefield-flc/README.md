# Furzefield Leisure Centre

## Build

```bash
mvn test
mvn package
```

## Run

```bash
java -jar target/furzefield-flc.jar
```

## UML

The editable PlantUML source is in `docs/uml/flc-system.puml`.

The exported diagram image is in `docs/uml/flc-system.png`.
